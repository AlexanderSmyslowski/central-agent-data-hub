# Agent Data Hub v0.1.21

Agent Data Hub v0.1.21 is a v1.0 adoption-readiness cleanup release.

## What Changed

- The public demo and configured local-ops startup paths are separated more
  clearly. Private operator seeds must live outside Git and be passed
  explicitly; the repository ships only neutral demo seed data.
- The public demo hygiene guard now checks for non-demo projects generically
  instead of carrying maintainer-specific project names.
- Repo-wide examples, tests, operator templates, and import examples now use
  neutral demo project names and placeholder paths.
- Hub View derives workspace areas from project metadata and visible counts;
  unknown projects stay unclassified instead of being guessed from their name,
  slug, or description.
- Workspace overview assembly now lives in a focused, database-independent
  module while Hub View keeps the same URLs, output shapes, and write boundaries.
- The mobile workspace view now brings its title, purpose, and first inventory
  figures into the initial 390 x 844 viewport without changing desktop density.
- `AGENTS.md` and operator templates use repo-relative or placeholder paths
  instead of machine-local maintainer paths.
- The v1.0 release path is now framed as an adoption-readiness gate: automated
  evidence is required, and the real human first-run proof remains a separate
  pre-tagging requirement.

## Boundaries

- no schema change
- no migration
- no new dependency
- no packaging change
- no hosted service
- no new Hub-memory write path
- no new review channel

## Release Position

v0.1.21 does not add capability. It makes the repository easier to evaluate as
an outside developer: the first public path is neutral, examples are anonymous,
and v1.0 remains defined as local reviewed-context infrastructure rather than a
finished product or automation platform.

## Human First-Run Evidence

The structured observation in `docs/first-run-test-protocol.md` has not yet
been run because no independent technical tester was available during this
release-candidate pass. This is an explicit deferral, not usability evidence.
The automated fresh-clone and external-developer checks prove executable paths;
they do not prove that a first-time person understands the product unaided.

This remains the only open v1.0 release gate.
Do not tag or broadly promote v1.0. Complete the protocol successfully with a
real technical tester first.

## Verification

```bash
git diff --check
bash -n scripts/*.sh
.venv/bin/python -m compileall agent_hub
.venv/bin/python -m pytest -q
scripts/smoke_public_demo.sh
scripts/v1_readiness_check.sh
.venv/bin/python -m agent_hub.cli status
.venv/bin/python -m agent_hub.cli check
```

Hub View must also be checked in a real browser at desktop size and at
390 x 844, with no browser-console errors.
